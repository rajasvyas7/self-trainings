<div class="jumbotran text-center">
  <h1 class="display-3">{{title}}</h1>
  <p class="lead">Jot down noites for your next video.</p>
  
  {{#if user}}
    <a href="/ideas/add" class="btn btn-dark btn-lg">Add Video Idea</a>
  {{else}}
    <a href="/users/login" class="btn btn-dark btn-lg">Login / Register</a>
  {{/if}}
</div>